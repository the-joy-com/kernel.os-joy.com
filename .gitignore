# uv builds the virtualenv here (named via UV_PROJECT_ENVIRONMENT=venv);
# it's reproducible from uv.lock, so never committed.
venv/

__pycache__/
*.pyc

# Server user persisted by deploy.sh on first run — local to each box.
.deploy-user

# Per-box secrets and config: database URL, symbiot email, HMAC secret, Gmail.
# .env.example is the committed template; .env itself never is.
.env

# GCP service-account key for the Gmail API — a credential, never committed.
*.gmail.json
gmail-credentials*.json
*-sa-creds.json

# The private half of the machine symbiot's persona — per-symbiot, never leaves the box.
# The public half (persona/public.md) is versioned; only the private slot stays local.
persona/private.md

# The login code dropped here on a mailboxless box (FileEmailClient, config.OTP_FILE).
# A short-lived secret the operator reads off the box — never committed.
OTP.txt

